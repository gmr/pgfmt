CREATE FOREIGN TABLE fdw_reporting.metrics (
    account_id   INTEGER        NOT NULL,
    total_amount NUMERIC(10, 2),
    avg_amount   NUMERIC(10, 2),
    generated_on DATE           NOT NULL
)
SERVER reporting_server
OPTIONS (
    batch_size '100000',
    schema_name 'report',
    table_name 'metrics'
);
